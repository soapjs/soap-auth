export enum StrategyType {
  Web3 = "web3",
  Local = "local",
  Token = "token",
  Basic = "basic",
  OAuth = "oauth",
  OAuth2 = "oauth2",
  ApiKey = "api-key",
  Session = "session",
}

export enum OAuthProviderType {
  Facebook = "facebook",
  Google = "google-oauth20",
  Twitter = "twitter",
  Spotify = "spotify",
  GitHub = "github",
}
