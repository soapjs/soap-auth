export enum StrategyType {
  Web3 = "web3",
  Local = "local",
  Bearer = "bearer",
  Basic = "basic",
  OAuth = "oauth",
  OAuth2 = "oauth2",
  ApiKey = "api-key",
  JWT = "jwt",
  Session = "session",
}

export enum OAuthProviderType {
  Facebook = "facebook",
  Google = "google-oauth20",
  Twitter = "twitter",
  Spotify = "spotify",
  GitHub = "github",
}
